const defaultState = {
    status: false,
    data: {}
};

function profile(state = defaultState, { type, payload = {} }) {
    switch (type) {
        default:
            break;
    }

    return state;
}

export default profile;
